# Resend API key — create one at https://resend.com/api-keys
RESEND_API_KEY=

# Where lead notifications are delivered (your inbox)
LEAD_TO_EMAIL=info@clearfix.de

# Verified sender. Until you verify a domain in Resend, you can use the
# shared sandbox sender below (works out of the box for testing):
LEAD_FROM_EMAIL=Clear Fix <onboarding@resend.dev>
